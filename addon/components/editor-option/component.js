import EditorToolComponent from '../editor-tool/component';

// Option of a Dropdown component used in an EditorToolbar
var EditorOptionComponent = EditorToolComponent.extend({
  classNames: ['editor-option']
});

export default EditorOptionComponent;
